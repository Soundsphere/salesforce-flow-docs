#!/bin/bash
## change project path to where the flow info is stored. The output files are then stored in the folder evidence
python -m automation_doc_exporter --project-root ~/path/to/the/flow/files

